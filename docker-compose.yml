
services:

  backend:
   build: ./server
   container_name: backend
   restart: always
   env_file:
    - ./server/.env
   ports:
    - "5000:5000"

  frontend:
   build: ./client
   container_name: frontend
   args:
      VITE_API_URL: http://13.238.217.27:5000
   restart: always
   ports:
    - "5173:5173"
   depends_on:
    - backend


